# 任务DAG设计

- source_url: https://v.douyin.com/0nXp04aNEts/
- method: vid2txt Playwright media extraction + local Qwen ASR
- transcript_chars: 2819

## Transcript

Hello，大家好，这里是永泽。今天我们再来讲讲我们任务的 DAG 的设计啊。之前我们也出过视频讲过，但是整体的效果不是很好，所以我们再出一个更详细的一个文档，结合着我们可能有一些代码来更细致的讲一下啊。我们之前也讲过我们关于我们项目的这么一个 React 处理啊，我们总共是2个，一个是动态的这么一个图结构啊，核心任务就是把任务拆成很多节点，然后每个节点代表1次推理、工具调用或者数据处理，然后根据依赖关关系组成任务图，也就是动态图结构，而不是固定的这么一个链路。这是一个例子啊，互相没有依赖的话就没有必要一个个执行，并所以就可以并发嘛。另外一个就是竞速机制，所以如果多个节点同性质的这么一个任务，比如说多个搜索，然后多个调用小模型的时候，那么就会并行执行。那么这样做有几个原因啊？第一就是 AI 系统本身不确定性，不同模型、不同工具的响应时间波动很大，那么串行执行会被最慢节点拖死。第2个就是并行能显著的降低整体任务的耗时。
第3个就是可以提升整体系统稳定性啊，第四就是扩展这么一个多级多 agent 的协作啊，多 agent 的本质也是一个图结构的调度问题。我之前已经出出过一个动态图 React 串行链式 React 啊啊，但是整体讲了还是比较了简略啊，给大家简单讲一讲，结合着我们的项目，我们具体它在我们项目里头定位，它在我们项目里头做什么，我们来讲一下啊。第一个就讲一讲 DAG 在我们真正的处于什么位置啊？那么它是在我们 React 的这么一个模式下啊，会出现这么一个 DAG 规划。第一个就是通过这个 Planner L M 进行这么一个图的这么一个规划。
生成这么一组 plan node，第2个就是并行加上竞速调度去进行这么一个调用啊，并行执行，最后由这个生成了 LLM 合成最终答案，综合所有观察。那么3个核心抽象，第一个就是 node，node 就是单个执行单元，工具调用子 agent 思考聚合啊，一共是四种种类。第2个就是 task graph，就是有向无环图带邻接表啊入度表拓扑层的这么一个缓存。第3个就 graph runtime，就是一个调度器啊，拓扑分层加上信号量并发加上竞速的这么一个执行啊，这是对应着我们前前面已经讲过了，一个是动态图结构，另外一个是这么一个竞速的机制。再看 node node 关键字段，这是整个整体这么一个 node 啊，ID 就是唯一标识嘛，type 呢，另外它是调用工具了还是调用这么一个子 agent，还是进行思考，还是进行聚合等等，name 等等，这个具体咱就不展开。那么超出长度归 DAG DAG 的这么一个字段，一个就是 task group，task group 就同组节点这么一个竞速执行啊，那么空代表独立，同 group 就代表竞速。
其余就首个成功的获胜，其余就被取消。第二就是 A 真 name 加上够，也就是说节点我们是可以是指 A 真的，撒播 A 真的，在我们这里已经给出了，也不只是工具。那么一共是三阶段这么一个执行链路，第一个阶段就是给到这么一个 planner L M，它进行这么一个规划啊，它规划成这么一个 JSON 节点数组，含这么一个依赖与竞速。第二就是构建这么一个根据它的生成 L M 的生成构建这么一个图，计算邻接表入度，检查是否有环悬空依赖。那么失败降级我们就全并行啊。第3个就是真正的进行这么一个执行啊，我们会有一个算法这么一个分层，那么每层呢就是按这么一个竞速 group 进行这么一个分组，然后信号量并发，每个节点都有状态机重试等等，再到最后了生成最终的这么一个答案。
再看端到端这么一个时序啊，就是端到端它的这执行过程，就是用户提问如果到这个 react 的模式了啊，它会首先进行这么一个 I I M plan graph 进行这么一个工具描述，JSON 描述生成这么一个 plan node 的这么一个 JSON 啊，对应着我们上面的这么一个执行链路，再到构建图，构建图之后它再到最后这么一个 E Q 的，我们这边有一个算法执行了这么一个并行的算法，对每1层有一个竞速机制这么一个分组，同 wrap 的话我们进行竞速，独立节点我们会进行普通的这么一个执行，信号量限制这么并发啊，剩下就是一些够了一些元部的一些东西，再到最后的这么一个执行单个节点，后把所有的结果进行这么一个到这么个聚合啊，把所有的观察给到这么一个最后的生成呀，之后它进行这么一个生成啊，最后生成最终答案。那么讲讲我们这个 D A G 独到之处，之前我们讲的时候也有人在评论为什么不用拉 graph 啦等等啊，讲讲我们就是说为什么不用拉 graph，或者说我们这个系统里头又做什么？第一个就是运行是 D A G 而并非是。
呃，编译时定义啊，首先 lograph 就它图结构是代码里头写死了啊，是在编译的时候就已经定义好了。那么我们呢是由这个，大家可以之前也已经看到了啊，是由这么一个 planner LM 它这么一个动态这这产出啊，就是同一个问题，不同的 LM 它输出的图可能完全不一样。那么好处就是它可以真正会规划啊。
代价就是一是需要降级路路径啊，二它每次 LM 可能输出不同啊，测试就比较难。第2个就是同层同层竞速啊，这也是我们独特一个性质性质呃独特的一个性质竞速机制啊。场景 X 是什么？我们可以同时调2个工具啊，谁先返回用谁。这是我们调度的一个细节啊，就在这里有一个通首个成功，那么就取消其余啊。好处就是延迟这么一个并发，延迟这么一个并发降低啊， n 路并发各路进行延迟，那么可靠性提也提升了。比如说我调一个工具它失败啊，但是说调另外一个同源的工具它成功了，那么直接拿同源这么一个工具就行了啊，避免这么一个单个失败之后我们就无法进行。再到最后一个就是这么一个拓扑分层，它单单步推进啊，language graph 就是每次推推进一个 step 状态机驱动，然后需要 condition 的意志决定下一步。那么我们使用这么一个可按算法进行这么一个分层啊，同层是自动这么并行啊，这是我们一个调度的这么一个伪代码。那么好处就是我们自动进行并行啊，开发者不用进行这么思考，延迟下界再到简单确定。
这就是我们简单的对我们我们细致的对我们项目的 D A D 设计进行了这么一个讲解啊，结合着我们之前已经有了啊，关于这个文档了，这次视频比不细致展开，因为上1次我们已经讲了，就是说这个是我们动态图 React 与串行劣势 React 的这么一个对比啊，主要讲了为什么要用动态图，动态图有什么关键组件啊，但是这个没有结合着我们项目来啊，今天我们节目结合着我们项目来讲一讲啊，今天我们的所有内容啊，包括这这是我们那个社区群友他们在评论啊，简单来说啊，那天我们在聊的时候，其实就是后排序2个图在那互相的比较嘛啊，大家可以结合着自己的一些理解啊，去更深刻的理呃理解一下。
