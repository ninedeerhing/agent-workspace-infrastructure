# 记忆系统详细介绍

- source_url: https://v.douyin.com/piGnMWFQqoA/
- method: vid2txt Playwright media extraction + local Qwen ASR
- transcript_chars: 4447

## Transcript

Hello，大家好，这里是永泽，今天我们来对我们的项目机器系统进行一个详细介绍。之前关于机器系统的讲解呢，其实收获了大家比较多的点赞哦，大家觉得我们机器系统还是设计的比较好的。之前也有对机机器系统有过一些文档，一些简单的文档，但是整体效果不是特别，不是效果不是特别好，还是说整体是比较简略了，大家看到是非常少。今天结合着我们的一些具体项目里面的代码，包括我们的整体架构来讲一讲我们的机器系统。
第一个就是整体架构，六槽位并行填呃填充，也就 Promod C T X 这个编排层呢，在之前也是如何组织机器上下文，这个我们已经讲过了啊，我们具体就不在这里展开了。那么主要就是五种的机器形态，大家可以去看一下五种机器形态。第一个就是短期对话窗口，也就是滑动窗口啊，那么机制就是滑动窗口啊，如果超过这么一个最大的限制的话，就直接丢弃，这是保存最近的这么一个对话记录。再到结构化偏好，结构化偏好是针对于用户的一个用户呢，他肯定说，比如说我这个用户我叫什么，或者这个用户我喜欢什么，我们可以把它设置为这么一个用户的偏好。那么它是一个在我们的系统是一个双通道写入啊，第一个就是规则路，就是同步零延迟，也就是说是固定化写入的。比如说信息里头有我叫呃永泽，它会直接把我叫永泽这个东西写入到这么一个结构里头啊，不需要那个大模型再去解析。另一部我们在规则路之外啊，还有一个大模型进行这么一个解析，抽这么一个 K V 的这么阶次结构，一共是2套通通道啊。
那么就是大模型的这么1条道是避免啊用户说完我叫张三立即问我问什么大模型还没有抽抽还没有抽取完成第二轮回答就会不知道单规则路呢那它覆盖率窄啊我们可能无限的扩展规则所以我们选择了这么一个双通道第一个就是保证这么一个即时一致性那么大模型保证了这么一个长尾覆盖率那么第3个就是长期的这么一个语义记忆啊我们刚才已经说短期记忆和偏好第3个就是长期语义记忆里头有比如说重要性召回时的综合分以及类别这个就是对应着我们上面如何组织记忆上下文这么一个类这么一个类别啊因为我们会有一个 solid filter 啊大家具体看可以看看上下文看这里头有通我们是通过这个来进行它这么一个过滤的所以我们在记忆系统这么一个长期记忆也有一个类别这么一个东西啊核心字段含义 importance 也就是重要性以及召回二级信号啊这是我们召回这么一个公式是它是随时间衰衰减的这个是我们的合并链路在后面会讲。
类别呢就是装配阶段过滤过滤维度，这个就是槽位的归属建议，CTX装配时会按此分流啊。第4个就是图增强层，说我们在短期记忆、长期记忆用户偏好之后，我们还进行这么一个图增强图增强。具体的这么一个图增强呢，在之前的RAG已经讲过了啊，为什么IG需要知识图谱啊？大家可以详情移步这个视频啊，大家如果说已经有我们这个文档，大家可以结合着看图增强层啊，这是on four g这么一个节点啊，里头有上1条记忆ID，用follows这么这么一个边以及它这个钩子，那边类型呢follows也就是语序相邻啊，前1条到当前similar to，也就是写入时实时计算它这么一个语义相似度自动建。那么关键能力就是图扩展呢，召回时不仅返回向量的这么一个命中了这么一个条目，而而且还延边这么一个扩1跳啊，具体这么一个实体之间的这么知识维护这么实体之间的关系，图中心这么一个合并这么一个保护，那么入度如果大于三大于等于三的这么节点就免于淘汰，说明它。
有很多其他一些记忆有关联啊，最后一个就是任务步骤的这么一个环形环形缓冲，这个就是在任务执行的时候，它会存一些任务短时执行这么一个记忆啊，那么任务周期生命周期就是跟大模型的 react 循环是一个绑定的，那么新任务开始时就会清空，那么任务创建就会进行这么追加啊，它是独立的，因为任务它这个步骤级是一个步骤级观察，它调用哪些工具的话，它会返回一些结果啊，还有一些，那么它调用工具的返回结果，它是1次性的，它和那个长期记忆是两种生命周期完全不同的东西，那么就不能进行混存。
然后来看看写入链路，也就是说在大模型进行这么一个回答之后，我们会进行这么一个记忆的写入啊，就是大模型回复。首先就是大模型回复的时候，我们会把这么一个短期记忆进行这么一个增加啊，因为它是一个滑动窗口维护的，进行这么一步抽取。首先就是大模型会抽这么一个 KV 的这么一个结构啊，以及这么一个规则路，对每 KV 进行这么分类管线，也就是刚才介绍这么一个类比啊，再到规则层，大模型进行这么一个兜底，进行这么一个向量化，进行内部的去重。最后如果未去重的话，那说明是新增的，我们直接把它这个保存下来。如果去重命中的话，我们可以进行这么一个合并啦，可以进行这么一个合并啊等等，就到最后的这么一个储存完成，一步这个。
我们的写入链路就是写入级分类嘛，刚才已经给大家说，这个 category 是类别是很重要，因为它代表着我们怎么去在 prompt 的 C T X 包进行这么一个召回啊。这是他们的一些规则层啊，我们同样是使用双通道，我们写入的时候会有一个双通道，就一个规则一个大模型录，那么这个也是有规则层，那么大模型兜底，大模型调1次大模型会返回这么一个 JSON。
嗯，那么为什么必须分类？如果没有分类的话，那么召回阶段就会使用 top k 啊。那么用户姓名和上位上次讨论菜谱而相似度竞争，那么姓名信息会被排 top k 以外。那么在之前这么一个弱组织机上面，我们已经讲过为什么要按这么类别进行分类，然后为什么要装配不同的槽位啊？这也是我们独特一个设计。那么有分类的话，那么身份信息就不算相似度。那么另外一个就是走相似，那么两路是互不干扰的。写入去重啊，这是一个隐式这么一个合并，这也是具体的代码，在这里就不跟不具体跟这代码来讲解。那么讲讲它的设计意图，设计意图就是去重，它不是丢弃啊，而是这么一个加固。那么多次提到这么一个事实，就会自然它的重要性会不断的累积。类别升级呢，遵循具体战争泛化啊，就会永远被更具体的类别进行覆盖。那么写入期就就会进行这么一个去重，再到异步建图，我们刚才已经说到我们有这么一个图记忆会进行这么实体之间关系的关联，这也是入口啊，这也是具体的一些代码。那么扫最后50条记忆，建这么一个 similar similar to 这么一个边啊，这么一个啊。
然后再讲一讲这么一个记忆的召回链路啊，记忆召回链路就是说三种召回策略对应三种的槽位啊，这也是跟我们如何组织上下文是有很大的。大家发现我们记忆的设计和这么一个如何组织上下文我们是相辅相成了啊，都是在 ProMode 的 C D X 包是一个相辅相成的这么一个概念啊，Text 也就是这么一个任务的这么一个执行器，然后 Recall 就是召回啊，召回方式向量加 T F 那个词义这么一个词袋兜底，然后加上图扩展，然后 Profile 也就是说它的那个偏好，然后会偏好以及它这么一个啊，大家可以在这里看看啊。
profile也就是它的偏好，然后稳定身份也就是用户画像啊，那么我们会按这么一个类别进行枚举啊，不算相似度，那么这一召回的这么一个综合分这个公式，第一个就是相似度啊，用户问我喜欢什么，那么我们会召回这么一个喜欢咖啡等等啊，第2个就是重要性，重要性就是次性好，然后相同语义下我们要把重要的先往前排啊，然后第3个就是不让这么一个重要性呃主导，就是防止啊我们之前很很久远的这么一个记忆达到这么一个主要的这么一个主要这么一个片段，然后第3个就是这么个solid这么一个过滤啊，这个在我们之前也也讲过啊，这个类别过滤了，然后这么个require text了，然后综合分这么一个法值，然后top k我们进行这么一个截断这么一个主呃主循环，我们会根据类别过滤等等。
那么注意1点呢，就是说 max age hours 年龄年龄硬过滤啊，按 create 这么一个创建时间进行这么一截断，然后 importance 衰减也是重要性衰减是一个软信号，那么召回时我们会刷新最后1次到达这么一个时间，那么访问触达就直直接重新激活，那么间接保证我们这么一个不断活跃的记忆不会合并而淘汰，然后再到图扩展啊，图扩展那么之前我们也已经讲过了，所以我们讲示意图示意图就是主动联想嘛，用户问上次咖啡，那么下两上回会拿到喜欢咖啡，那么会补到那么图层会这么根据实体关系之间的这么一个跳跃，然后补回许许多相邻这么一个昨晚熬夜了，发现这么一个间接关联但不直接相似的这么一个历史啊，第6个就是合并链路，合并链路肯定就是最复杂的链路，就是衰减去重合并过期等等啊。
这是一个出这是一个触发器啊，然后调用这是对应的这么一个在记忆中这么一个调用点，那么它是一个计数触发啊，并不是定时进行这么一个触发，那么就是防止时低活跃期，就是说用户没有问进行问答的时候我们不空转，那么高活跃期呢，我们达到这么一个计数器之后，我们会进行这么及时清理，防止这么一个重复条目 的这个堆积，第一个就是指数衰减啊，这是具体的代码，那么我会按这么一个创建时间，然后并非上次衰减时间进行这么一个衰减啊，这方式是幂等，第2个就是日衰减系数啊，第3个就是说控制这么一个 PG 写放大，就是防止它衰减力度小，我们在这个 PG 进行，就是我们用 PG 缓存记忆啊，也防止它进行这么一个衰减，然后就是驱虫加合并啊，它是一个双阀值这么一个分流。
比如说这个隐抛这个像素大于零呃大于等于0.95，然后在0.80~0.95之间啊，那么它的细节就是重要性高了它是为基础啊，重要性高就是去重是保留重要性高的重要性高它是一个基础，然后我们取最最高的重要性，然后我们隐抛顶这个向量啊就是按重要性进行这么一个加权平均，然后 last x 也就最后1次到达时间，然后是刷新到这么一个最新的这么一个时间，然后合并为什么不调这个大模型啊？就是防就是确定性嘛，可低延迟可单测等等，就是因为大模型它是黑盒代码嘛，我们每次调它会出出不一样的这么一个结果，最后就是双门槛的这么一个过期淘汰啊，这也是我们一个一个代码，就是具体的话呃单从文档来看大家还看着比较抽象，还是具体的话大家在我们代码里头贴出来代码，根据代码进行这么一个呃相似的这么一个学习，就是双门槛一个关系啊，就是淘汰的关系就是它的时间已经超过这么一个30天，然后同时它的重要性已经小于0.3我们才删，那么防就是保留那些。
虽然说时间已经超过30天，但是它是重要性很高了，那么永久保留，那么防止是就是单方面我们到期就删，比如说我在30天之前说我叫永泽，那可能说30天之后如果到期之后啊，他已经忘了我叫永泽了。
这一个机器系统详细的我们呃项目的机器系统的介绍啊，那么主要是为之前这么一个机器系统介绍打了一个补丁，那么后续我们也会不断的继续继续出相应的文档嘛，之后可能会出一个 RAG 相关了，就是继续详细的介绍介绍我们项目，因为我们马上要上新项目了，我们要把这个老项目做一个总结一个补档，谢谢大家的关注。
