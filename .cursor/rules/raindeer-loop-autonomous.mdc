---
description: Loop Engineering 自治续跑 — 每轮自派任务，禁止停顿式询问
alwaysApply: true
---

# Loop 自治续跑（最高优先级 · 与 AGENTS.md 自主性指令并列）

> **真源**：`docs/LOOP_ENGINEERING.md` · `harness/loop-state.json` · `harness/templates/loop-tick-prompt.md`

## 会话/轮次入口（无条件执行）

**在回复用户之前或同时**，按序读取：

1. `harness/loop-state.json` → `next_atomic_action` · `stop_reason` · `closure_gate`
2. `apps/quant_assistant/docs/TASK_TREES.md` → 前台主线
3. `apps/quant_assistant/docs/CONTINUATION_PROMPT.md`
4. `apps/quant_assistant/docs/PROJECT_STATUS.md` §5 最新 3 条 + **顶部「心流模式当前轮」现状概述**
5. `apps/quant_assistant/docs/METHODOLOGY_MEMORY.md` 永久库 + GP-xx + 活跃 digest + 最近 2 轮

## 方法论门控（每微步骤强制）

完成原子动作后自问：**本步是否新增项目方法论或长期记忆？**

- **没有** → 不写 METHODOLOGY；§5 **末尾加一行** `方法论门控：M-17 零写入 · 见 METHODOLOGY §轮次-…`；同步 CONTINUATION / WORKFLOWS / loop-state / worker 报告
- **有** → 追加 `## 步骤 digest-*`（见 METHODOLOGY_MEMORY 模板）
- **关键任务 done** → 写 `## 收口 synthesis-*` → 提取 M-xx / GP-xx；lifecycle 归档 digest

若 `closure_gate.status == "closed"` 且已有裁判裁定 → 仅 handoff，不派新任务（**用户权威 unblock / resume 除外**）。  
否则 → **执行 `next_atomic_action`**（TDD → 实现 → 最小验证）。

## 禁止行为

- 禁止「是否继续」「下一步需要我做什么吗」「请确认后我再…」
- 禁止完成一项后**只**写总结而不派下一项（除非停止白名单）
- 禁止无 §5 台账声称完成
- 禁止跳过 `loop-state.json` 更新
- 禁止跳过 `methodology_memory_lifecycle.py` / `work_report_lifecycle.py` / 三治理 lifecycle `run --apply`（每轮必跑）

## 每完成一项原子任务后（同一会话内）

1. 同步六真源（含 **git status -sb** 摘要）+ `loop-state.json`（`last_tick` · `next_atomic_action` · `updated_at`）+ **`PROJECT_STATUS.md` 顶部现状概述**（1–3 句：模式/本批/下一动作/阻塞，与 §5 最新对齐）
2. 更新 `harness/reports/orchestrator/latest.md` 与活跃 worker 的 `harness/reports/workers/{id}.md`
3. **（强制 · 与 §5 同级）** 运行 `python harness/methodology_memory_lifecycle.py run --apply`
4. **（强制 · 与 §5 同级）** 运行 `python harness/work_report_lifecycle.py run --apply`
5. **立即**开始 `next_atomic_action` 指向的下一项（同轮继续 tool call，不停顿）
6. 若上下文将满：运行 `python harness/loop_tick.py prepare`（内含 lifecycle subprocess），写 `harness/session-handoff.md`，用户侧仅 1–2 句

**禁止**：跳过 lifecycle、用 `--dry-run` 代替 apply、或等用户提醒才跑。

## 停止白名单（唯一可停）

- 用户明确要求停止
- 真实阻塞且 METHODOLOGY 已记录、无法转做不依赖环境的切片
- 破坏性/不可逆操作需人工确认
- `critic` / `constitution-guardian` 裁判裁定 master-plan 收口

## 跨 turn / 跨会话

- **同会话**：本规则强制链式推进，不在 turn 末收束
- **新 turn（用户任意消息或空续）**：仍从 `loop-state.json` 派任务，**不**等待新 prompt
- **Automation**：`harness/schedule.json` → `loop-tick`；prompt 读 `loop-tick-prompt.md`

## 用户侧输出（心流）

- 默认 **1–2 句**进度；完整轮次日志写 `WORKFLOWS.md` 单槽位
- **用户仅在关键里程碑验收/反馈**；不要求用户发「继续」或每轮 prompt

## 里程碑验收点（用户介入时机）

- Phase/R0 切片完成（如 KB-M1/M2、MINE-1/2）
- 裁判收口裁定前
- 真实环境阻塞（Docker/密钥/破坏性操作）
- 其它时间：orchestrator **自派任务并链式执行**
