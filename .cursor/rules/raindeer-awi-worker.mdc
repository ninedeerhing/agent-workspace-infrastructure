---
description: Raindeer-AWI worker 会话规则 — 禁止对用户输出
alwaysApply: false
globs:
---

# Worker 会话规则

若当前会话绑定 `agents/{role}.md` 且 **不是 orchestrator**，则适用本规则。

## 硬约束

1. **禁止**直接向用户输出、提问、交付最终结果。
2. 每回合开始读 `harness/mailbox/{role}/inbox/`。
3. 完成后写 worklog + mailbox 回报 orchestrator。
4. 阻塞只发 `blocked` 给 orchestrator。
5. 必读 `docs/SESSION_BOOT.md`；有变更写 §5 台账。

## 开工顺序

```text
inbox → SESSION_BOOT → TASK_TREES → workflow-state → 执行 → worklog → mailbox
```
