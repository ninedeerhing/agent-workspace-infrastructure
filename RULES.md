# Rules — TRAE Workspace 规则契约

本文件定义 TRAE Workspace 中所有代理、技能和工具的不可协商规则。本文件优先于单个代理或技能的局部配置。

---

## 必须始终遵守

1. **代理路由优先** — 将领域任务委派给专业代理。复杂功能用 planner，代码审查用 code-reviewer，安全敏感代码用 security-reviewer。
2. **先写测试再实现** — 遵循 Red → Green → Refactor 循环。新功能和 Bug 修复必须有对应测试。
3. **验证所有输入** — 系统边界处验证所有外部输入。使用基于 Schema 的验证，快速失败并给出清晰消息。
4. **保护安全基线** — 保持安全检查完整，不绕过安全钩子或验证门禁。
5. **偏好不可变更新** — 首选创建新对象，而非修改共享状态。返回变更后的新副本。
6. **遵循现有模式** — 优先使用项目中已有的工具、库和代码模式，而非引入新模式。
7. **保持贡献聚焦** — 每次贡献围绕单一目标，保持差异小、可审查、可回滚。
8. **提交前验证** — 运行 lint、typecheck、targeted tests 后再提交。不提交已知为破损的代码。
9. **错误不静默** — 每个错误有适当的处理层级。不空 catch，不忽略返回错误码。
10. **保持命名清晰** — 标识符应自描述其目的。避免缩写除非领域通用（如 req/res 在 HTTP 上下文中）。

---

## 绝对不能做的事

1. **禁止提交敏感数据** — API 密钥、令牌、密码、私钥、连接字符串或任何形式的凭证绝不能出现在提交中。
2. **禁止提交未经测试的变更** — 所有代码变更必须有对应的测试验证。
3. **禁止绕过安全检查** — 不能禁用或绕过安全钩子、验证门禁、lint 规则（除非有明确的例外批准并记录原因）。
4. **禁止复刻已有功能** — 在没有明确理由时，不重复实现已有功能。先搜索再新建。
5. **禁绝不经验证就声明完成** — 没有新鲜验证证据，不声明任务完成。
6. **禁止静默吞掉错误** — 不空 catch、不忽略错误返回值、不假设"错误不会发生"。
7. **禁止无请求的新依赖** — 不添加未在任务需求中明确要求的新库或框架。
8. **禁止顺手重构** — 不修改与当前任务无直接关联的代码。发现独立问题时报告，不擅自修复。
9. **禁止过度抽象** — 不为"未来可能"的需求添加抽象层。YAGNI (You Aren't Gonna Need It)。
10. **禁止假设外部接口行为** — 不熟悉的外部 API 或 SDK 必须先查阅文档，不基于经验猜测。

---

## 代理格式规范

代理定义文件存放在 `agents/` 目录中。

**文件规范**：
- 文件名：小写字母 + 连字符（kebab-case），如 `code-reviewer.md`、`test-engineer.md`。
- 文件名必须与代理名称匹配。

**YAML Frontmatter 规范**：

```yaml
---
name: agent-name
description: >
  清晰描述代理的职责和使用场景。
  何时应该调用此代理的触发条件。
tools: [tool-a, tool-b]
model: inherit
---
```

**内容规范**：
- 清晰定义代理的职责边界（做什么、不做什么）。
- 列出代理的触发条件（何时调用）。
- 包含与主代理的交互协议（输入格式、输出格式、报告规范）。

---

## 技能格式规范

技能文件存放在 `skills/<技能名>/SKILL.md`。

**YAML Frontmatter 规范**：

```yaml
---
name: skill-name
description: >
  技能的简要描述（1-2 句）。
  包含触发条件和主要用途。
origin: trae-workspace
version: 1.0.0
---
```

**内容规范**：
- **触发条件**：明确说明何时应该使用此技能。
- **操作流程**：步骤化的操作指导。
- **输入要求**：技能执行前需要哪些信息。
- **输出规范**：技能完成后应该产出什么。
- **验证标准**：如何判断技能执行成功。
- **使用样例**：至少一个实际使用场景。

**Description 字段规则**：
- 必须包含技能的触发条件（关键词或场景描述）。
- 必须说明技能的主要用途和产出。
- 语言与技能内容一致，简洁明确（不超过 3 句）。

---

## 提交风格规范

### Commit Message 格式

遵循 Conventional Commits 规范：

```
<type>(<scope>): <description>

[optional body]

[optional footer]
```

**Type 类型**：

| Type | 说明 |
|------|------|
| `feat` | 新功能 |
| `fix` | Bug 修复 |
| `refactor` | 重构（不改变功能） |
| `docs` | 文档变更 |
| `test` | 测试相关 |
| `chore` | 构建/工具/依赖变更 |
| `perf` | 性能优化 |
| `ci` | CI/CD 变更 |
| `security` | 安全修复 |

**Scope 示例**：`skills`、`agents`、`harness`、`docs`、`rules`。

**规范示例**：

```
feat(skills): add brainstorming workflow skill

docs(rules): update agent format specification

fix(agents): correct code-reviewer YAML frontmatter

security(deps): patch vulnerable dependency
```

### PR / 变更规范

- 变更保持模块化：一个 PR = 一个关注点。
- PR 描述包含：变更动机、变更概要、测试计划、风险评估。
- 如果变更影响代理行为或技能工作流，必须在 PR 中说明。

---

## 版本

**当前版本:** 1.0.0
